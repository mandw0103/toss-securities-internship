# 홈 > 차트 섹션 리스트 FE 스펙

> 기획서: [docs/planning/home/04-리스트.md](../../planning/home/04-리스트.md)
> 작성일: 2026-05-10
> 상태: 승인됨

## 개요

홈 차트 섹션 하단의 종목 리스트. [03-필터.md](./03-필터.md)의 시장/정렬/기간 선택값에 따라 100개 종목을 정렬·필터링한다. 이슈 13에서는 토스증권 홈 실시간 차트 랭킹 API를 최초 1회 호출해 `biggest_total_amount + realtime + all` 기준의 기본 랭킹 데이터를 노출한다. 정렬 기준에 따라 컬럼 구성이 동적으로 변한다 (8개 ↔ 7개).

## 컴포넌트 구조

```
ChartSection                       # 02-탭 스펙에서 정의된 wrapper
└── ChartSectionList                # 본 스펙
    ├── ChartListHeader
    └── ChartListRow × N            # N = 시장 필터 적용 후 항목 수 (최대 100)
        └── Logo                    # 01-지표와 공유
```

## 컴포넌트 명세

### ChartSectionList

| 속성 | 타입 | 필수 | 설명 |
|------|------|------|------|
| stocks | Stock[] | O | 원천 데이터 (`useStocks()` 결과) |
| filter | ChartFilterState | O | 시장/정렬/기간 |

**동작:**
1. `applyChartFilter(stocks, filter)`로 정렬·필터를 적용.
2. `<ChartListHeader sort={filter.sort} />` 렌더.
3. 결과 배열을 인덱스+1을 `rank`로 부여하여 `<ChartListRow>` 100개(또는 시장 필터 적용 후 수)로 렌더.
4. 결과 0건이면 별도 안내 없이 빈 영역으로 노출.

### 정렬·필터 헬퍼 (`src/utils/stocks.ts`)

```ts
export function applyChartFilter(stocks: Stock[], filter: ChartFilterState): Stock[];
```

**동작:**
- `filter.market`이 `'국내'` 또는 `'해외'`이면 해당 시장만 남기고, `'전체'`이면 필터링 없음.
- 다음 매핑으로 `metricsByPeriod[filter.period]` 기준 정렬:

| sort | 비교 키 | 방향 |
|------|---------|------|
| 토스증권 거래대금 | `tossTradingValue` | desc |
| 토스증권 거래량 | `tossTradingVolume` | desc |
| 거래대금 | `totalTradingValue` | desc |
| 거래량 | `totalTradingVolume` | desc |
| 급상승 | `changeRate` | desc |
| 급하락 | `changeRate` | asc |

- 결과 0건이면 빈 배열 반환.

### ChartListHeader

| 속성 | 타입 | 필수 | 설명 |
|------|------|------|------|
| sort | SortKey | O | 컬럼 구성 결정용 |

**컬럼 구성:**

거래대금/거래량 계열 (`토스증권 거래대금` / `토스증권 거래량` / `거래대금` / `거래량`) — **8개 컬럼**:

| # | 컬럼 헤더 |
|---|-----------|
| 1 | 순위 |
| 2 | 로고 |
| 3 | 종목명 |
| 4 | 현재가 |
| 5 | 등락률 |
| 6 | `<sort> 순` (예: `토스증권 거래대금 순`) |
| 7 | 토스증권 거래 비율 |
| 8 | 토스증권 AI 요약 |

등락률 계열 (`급상승` / `급하락`) — **7개 컬럼**:

| # | 컬럼 헤더 |
|---|-----------|
| 1 | 순위 |
| 2 | 로고 |
| 3 | 종목명 |
| 4 | 현재가 |
| 5 | 등락률 순 |
| 6 | 토스증권 거래 비율 |
| 7 | 토스증권 AI 요약 |

### ChartListRow

| 속성 | 타입 | 필수 | 설명 |
|------|------|------|------|
| rank | number | O | 1부터 시작 |
| stock | Stock | O | 종목 데이터 |
| period | Period | O | 등락률·동적 metric 추출 |
| sort | SortKey | O | 컬럼 구성 결정 |

**셀별 동작:**

| 위치 | 표시 값 |
|------|---------|
| 순위 | `rank` |
| 로고 | `<Logo logoSeed={stock.logoSeed} />` ([01-지표.md](./01-지표.md) 정의) |
| 종목명 | `stock.name` |
| 현재가 | `stock.currentPrice`. 천 단위 콤마 포맷 + **`₩` 접두**. 시장 구분 없이 항상 `₩` 사용 (예: `₩78,900`, `₩245.32`) |
| 등락률 / 등락률 순 | `metricsByPeriod[period].changeRate` (%). **양수에는 `+` 부호 prepend** (예: `+1.23%`), 음수는 부호 그대로 (예: `-1.23%`), 0%는 `0.00%`. 색상은 양수 빨강(`#f04452`)·음수 파랑(`#3182f6`)·**0%는 기본 텍스트 색상** |
| 6번째 컬럼 (거래대금/거래량 계열일 때만) | `sort`에 매핑된 metric 값 (`metricsByPeriod[period].{tossTradingValue \| tossTradingVolume \| totalTradingValue \| totalTradingVolume}`). **천 단위 콤마** 포맷 |
| 토스증권 거래 비율 | `stock.tossSharePercent`가 `number`이면 `%`로 표시, `null`이면 빈 값 |
| 토스증권 AI 요약 | `stock.aiSummary` (한 줄, 넘치면 말줄임) |

> 이슈 13 API 응답에는 `tossSharePercent`, `aiSummary`가 없으므로 각각 `null`, 빈 문자열 `''`로 매핑한다. 화면에서는 빈 값으로 보이도록 처리한다.

**행 동작:**
- 행 클릭 시 `useNavigate('/stocks/{stock.stockId}')`.
- `/stocks/:stockId` 라우트는 [01-지표.md](./01-지표.md)에서 등록한 placeholder를 공유.

## API 연동

[common/02-주식데이터.md](../common/02-주식데이터.md)의 `useStocks()` Query를 [01-지표.md](./01-지표.md)와 공유한다. 이슈 13부터 `useStocks()`는 토스증권 홈 실시간 차트 랭킹 API를 호출해 받은 `Stock[]`를 전달한다.

### 이슈 13 고정 요청

```json
{
  "id": "biggest_total_amount",
  "filters": [
    "KRX_MANAGEMENT_STOCK",
    "MARKET_CAP_GREATER_THAN_50M",
    "STOCKS_PRICE_GREATER_THAN_ONE_DOLLAR"
  ],
  "duration": "realtime",
  "tag": "all"
}
```

### 이번 사이클 제한

- 기존 필터 UI는 유지하되, API payload를 필터 선택값에 맞춰 동적으로 바꾸지 않는다.
- 시장 필터는 `productCode` prefix로 추정한 `Stock.market` 값을 사용한다.
- 기간 선택값도 API `duration`에 반영하지 않고, API는 항상 `realtime`으로 호출한다.
- SSE, polling, 주기적 갱신은 구현하지 않는다.
- API 응답에 없는 `토스증권 거래 비율`, `토스증권 AI 요약`은 빈 값으로 처리한다.

## 상태 관리

### 서버 상태

- 본 컴포넌트는 `useStocks()`를 직접 호출하지 않는다.
- 흐름: HomePage(`useStocks()` 1회 호출) → ChartSection → ChartSectionList. 자세한 호출 위치는 [01-지표.md](./01-지표.md) 참조.
- `useStocks()`는 `staleTime: Infinity`, `refetchOnWindowFocus: false`로 최초 조회 값을 유지한다.

### 클라이언트 상태

- 자체 상태 없음.
- 필터 상태는 [02-탭.md](./02-탭.md)의 `ChartSection` 부모 `useState<ChartFilterState>`에서 props로 전달받는다.

### 파생 데이터

- `applyChartFilter(stocks, filter)` 결과를 `useMemo`로 메모이즈 (입력: `stocks`, `filter.market`, `filter.sort`, `filter.period`).

## 예외 처리

| 상황 | 처리 |
|------|------|
| 시장 필터 결과 0건 | 별도 안내 없이 빈 영역 |
| 랭킹 API 요청 실패 | 리스트 영역에 `API 요청 실패` 텍스트 노출 |
| 랭킹 API 응답 구조 불일치 | 리스트 영역에 `응답 구조 불일치` 텍스트 노출 |

## 라우팅

`/stocks/:stockId` — [01-지표.md](./01-지표.md)에서 등록한 placeholder 라우트를 공유.

## 파일 구조

```
src/
├── apis/
│   └── tossRanking.ts                 # 랭킹 API 호출 및 Stock 매핑
├── utils/
│   └── stocks.ts                       # applyChartFilter 추가 (selectRecommended와 동일 파일)
└── components/
    └── home/
        └── chart-section/
            ├── ChartSectionList.tsx
            ├── ChartListHeader.tsx
            └── ChartListRow.tsx
```

## 체크리스트

- [x] 컴포넌트 구조 확정
- [x] 정렬·필터 매핑 확정
- [x] 동적 컬럼 구성(8↔7) 확정
- [x] 이슈 13 랭킹 API 고정 요청 확정
- [x] API 응답 미포함 필드 빈 값 처리 확정
- [x] SSE 제외 및 최초 1회 조회 범위 확정
- [x] 민서 승인 완료

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-06-11 | 이슈 13 홈 실시간 차트 랭킹 API 연동 범위 정리. `biggest_total_amount + realtime + all` 고정 요청, 기존 필터 API 미연동, 빈 값 처리, SSE 제외 명시 |
| 2026-06-11 | 랭킹 API 요청 실패 시 `API 요청 실패`, 응답 구조 불일치 시 `응답 구조 불일치` 텍스트를 리스트 영역에 노출하도록 확정 |
| 2026-06-11 | 시장 필터는 API `productCode` prefix 기반으로 추정한 `Stock.market` 값을 사용하도록 확정 |
| 2026-06-11 | 민서 승인에 따라 상태를 `승인됨`으로 변경 |
